<section class="section" style="text-align:center">
  <div class="container">
    <span class="eyebrow">404</span>
    <h1 class="display-2">{{ 'general.404.title' | t }}</h1>
    <p class="lede mx-auto">{{ 'general.404.subtext' | t }}</p>
    <div class="hero-actions" style="justify-content:center; margin-top: var(--space-6)">
      <a href="{% if collections.bags != blank %}{{ collections.bags.url }}{% else %}{{ routes.all_products_collection_url }}{% endif %}" class="btn btn-primary">Shop All Bags</a>
      <a href="{{ routes.root_url }}" class="btn btn-outline-dark">Back Home</a>
    </div>
  </div>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
