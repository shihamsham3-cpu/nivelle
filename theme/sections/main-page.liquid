<header class="page-header">
  <div class="container">
    <span class="eyebrow">{{ shop.name }}</span>
    <h1>{{ page.title }}</h1>
  </div>
</header>

<section class="section">
  <div class="container">
    <div class="page-content rte mx-auto" style="max-width: 68ch">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
